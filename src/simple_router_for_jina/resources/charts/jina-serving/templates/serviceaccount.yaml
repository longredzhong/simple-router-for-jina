{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "jina-serving.serviceAccountName" . }}
  labels:
    {{- include "jina-serving.commonLabels" . | nindent 4 }}
automountServiceAccountToken: false
{{- end }}
